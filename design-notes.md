# Design notes

## ドキュメント概要
foley-forge（プロトタイプ）開発に関する日々の設計メモを記載する。

## 反省
### 2026/5/28 
#### やったこと
- バックエンドにComfyUIのGPU最適化を採用するか検討
- 開発環境でDockerを採用するか検討
- 観測ポイントと評価ポイントの設計方針を定義
#### 気づき
- 観測ポイントと評価ポイントを定義することが大事。
- 評価ポイントにおける「評価軸」の定義も行う必要。
- 観測する方法、評価をする方法（ツールなど）を選定する必要。
- Dockerは不採用にする。
    - ユーザはエンジニアではないことが想定される。なので、Docker環境を整えることが難しいと考える。
    - また、バックエンドの最適化（本アプリでは、パラメータ調整など）を行う上で、Dockerでのデバッグがやりづらいと考える。
    - 配布に関してはDockerが良いとは考えるが、当分は、ユーザに使用してもらうことを想定し、非Docker環境で開発を行う。
    - 今後、OSSとして開発が進むようであれば、Dockerでの配布を検討する。
- ComfyUIは採用しない。
    - 確かに、ComfyUIは生成エンジンとして、かなり有用であるとは考える。
    - ただし、生成エンジンのみでComfyUIを採用するのは、ユーザにComfyUIの学習を強いることになってしまう。
    - ComfyUIは様々なワークフローを自動化できるアプリケーションではあるのに対して、本アプリケーションはSE生成に特化したアプリの立ち位置となるため、ComfyUIに依存しない方針でいくことにする。
- LLMの生成物の評価について、現職で何が一番良いかを評価するために評価軸をどのようにするかという議論があった。
    - ただその時は、生成物について、評価の高いものただ一つを出力するようなアーキテクチャをとっていた。
    - しかし、よく考えてみれば、評価の高い生成物をいくつか出力して、その中からユーザが選択できるようにするアーキテクチャの方が良いのではないかと思った。
--------------
### 2026/5/30
#### やったこと
- 構造LLMのプロバイダーを選択できるようにするか検討
- 拡散モデルのプロバイダーを選択できるようにするか検討
- 未決定事項およびTODOの管理方法を議論
#### 気づき
- Opus4.8が賢すぎてびっくりした。
- 先週まで話していたOpus4.7とは、まるで別人と話しているような気がした。
- ただし、ドキュメントを作成してもらった時に、人間が期待するドキュメントに記載する粒度感とLLMが実際に作成するドキュメントの粒度感が少しずれている気がした。
    - 特に、「情報量の多さ」にズレがある気がした。
    - ここは生成物に対して、追加指示をして微調整する必要があると感じた。
    - もしくは、事前にドキュメントの粒度に関して、認識を合わせておく（プロンプトでサンプルのドキュメントを示しておく）ことが必要かもしれない。
    - AIは人間の理解を超えて、生成物を作成しているように見えるから、人間がそこを調整していかないといけないような気がする（ただし、AIの方が賢いことは事実だからそこは注意）。
- ディレクトリごとのREADMEは必要かも（ただし、肥大化してきたらで良い）。
    - ディレクトリの概要と、そのディレクトリ内のドキュメントの役割など。
    - もちろん、人間が見てわかるようにする目的もあるが、LLMの参照を助けるという役割もある。
    - ただし、LLMが理解するんだったら、READMEとかで示すのではなく、YAMLとかJSONの方が良いかもしれない...
- LLMにGitHubのリポジトリを触らせる場合（ghコマンドなどで）は、必ず、コマンド許可設定を作成する。
- 本アプリの目的のところで、「複数バリエーションから最適な音を選べる体験を提供」に関して、最後に人間が選択することで、生成過程でAIの創造力を活用し、最後に人間の創造力を使っているので、非常に良い立ち位置だと思った。
--------------
### 2026/5/31
#### やったこと
- GitHubのIssue運用ルールの策定
- Claude Codeのpermissionsの検討、定義
#### 気づき
- GitHubのIssue運用ルールの策定
    - あくまで個人開発であるので、ルールはガチガチに決めないようにした。
    - ただしこれが、業務での開発になると、ルールはかなりガチガチにする必要が出てくると思う。
    - **開発者が増えれば増えるほど、ルールの変更は難しくなるので、最初にしっかりと決めておくことが大事**だと思う。ここは注意しないといけない。。。
- Claude Codeのpermissionsの検討、定義
    - Claude Codeが不意に、マシンに保存されているトークンを流出させないようにするためにpermissionsを設定。
    - ただし、これだけだと不十分だと感じ、sandbox（ネットワーク設定とファイルアクセス制限）を設ける必要があると感じた。
    - ただし、permissioinsとsandboxを組み合わせても完全にトークンの流出を抑えられるわけではない（TLS問題など）。
    - さらに対策するなら、万が一トークンが流出した際に、トークンの有効期限を設定するなどの運用ルールが必要であると感じた。
    - また、**今回は内部からトークンが流出する場合を考えたが、パッケージ侵害などの外部からの攻撃によってトークンが流出する場合も考える必要**がある（VSCode拡張機能インストールやnpm経由でのインストールなど）。
--------------
### 2026/6/1
#### やったこと
- LLMのGit操作をどこまで任せるかを検討
- Claude Codeのpermissionsとsandboxの設定
- Claude CodeのSkillの導入検討
- 観測ポイントと評価ポイントを議論
#### 気づき
- LLMのGit操作をどこまで任せるかを検討
    - LLMにコミットまではさせて良い気がした。コミットは戻せる。
    - コミットメッセージを考えるのはLLMの方が的確だし、人が考えるのは非効率な気がしてきた（ただし、コミットメッセージの書き方など、ルールは決めておく必要あり）。
    - LLMにpushはさせない。
- Claude Codeのpermissionsとsandboxの設定
    - とりあえず、ガチガチに組まずに最小限の設定をした。
    - **設定が効いているかどうかのテストをするスクリプトを用意した方が良いのかも**しれない。 
    - Claude Codeのallow/ask/denyに入れるべきコマンドの選別する判断軸を何か設定しようとしたが、決められなかった。
    - ここは、おそらく**明確な答えはなく、個人やチームで話し合った運用ルールが正解になる**気がする。
    - どの人でも、どのチームでも通用するようなルールがあるわけではない気がした。
    - **ただし、どの人、どのチームでも、最低限、必ず禁止しなければならないdenyを設定することは必須**であると感じた。
    - allowに設定すれば、効率化が図れるくらいだから、allowを決めるのは、設計順位が低い気がする。
- Claude Codeの設定は、ドキュメント化しなくても、setting.jsonに記載されているから、作成しなくても良いと思った（無駄なドキュメントは減らしたい）。
- Claude CodeのSkillの導入検討
    - Skillを作成しようとしたがやめた。
    - 理由としては、プロジェクトのタスクがまだ定まっていない状態で、Skillを設計すると、Skillの粒度や内容が実際のタスクと合わない気がした。
    - 最初から完璧なSkillは作れないから、まずはプロンプトで対応して、実際のタスクが増えてきてから、Skillに切り出しても良いと思った。
- 観測ポイントと評価ポイントを議論
#### 思ったこと（哲学）
- このアプリ生成物は最終的に、人間の感性によって選ばれたものが正解となる。
    - この人間の感性（なんとなくいいみたいな言語化できないこと）は、もちろん数値化なんてできない。
    - つまり、アプリ全体で見たら、ユーザ入力と、システムから出てきた生成物を選ぶ部分で、人間の創造性が入り込む。
- 一方で、AIの創造性を入り込む余地も作る必要がある。
- 個人的な理想は、PID制御のようなシステム
    - PID制御は対象のシステムがブラックボックスでも、PIDゲインを調整すれば、理想値に出力値を近づけることができる（追従できる）。
    - これを本システムに置き換えれば、理想値が人間の入力。出力値が本アプリケーションの生成物。対象のシステムがLLM、Text to Audioモデル。
    - ただし、注意しなければいけないのは、本来の産業システムは理想値は数値化でき、出力値も数値化でき、観測できる。そしてその値を使って、数式を元に計算することができる。そして、理想値に近づけることができる。
    - 本システムは理想値は数値化できず、出力値も数値化できない。ここがむずかしい。
    - ただし疑問として、人間の感性を数値化する必要があるのか。ない気がする。。。
- もう一つ議論したいのは、PIDゲインのようなパラメータをどのように調整するのかという話。
    - 実際の産業システムでは、PIDゲインを人間が調整する場合もあるが、システムが自動で調整する場合がある。
    - 我々としては、第１フェーズでは開発者がバックエンドのパラメータを手動で調整する。第２フェーズでパラメータを自動で調整する機構を開発したい。
- そして最終的に、**モデルを差し替えても、ユーザの理想値に追従する生成物を出力できるアプリケーション**にしたい。
- あと、生成物の品質のデバッグはどうするのかという問題がある。
    - 個人的に、**出力物の品質のデバッグは、開発者は不要と感じている。**
    - ただし、**生成物の音質に問題があったりなど、定量的に判断できる部分はデバッグの必要があると感じる。**
    - なぜなら、開発者の創造性ではなく、ユーザの創造性に合わせていく必要があるから。
    - デバッグをユーザに任せるというのが良い気がする。
    - ユーザが生成物に対して、選択することで、バックエンド側が傾向を分析して、パラメータを自動調整。
    - 生成物に対して、ユーザがコメントをつけてサーバーに送信することで、バックエンドが解析して、バックエンドのパラメータを自動調整。
--------------
### 2026/6/2
#### やったこと
- 個人のパーソナライズする機構を入れるかどうかを検討
#### 気づき
- Claude CodeのSkill設計
    - コミットメッセージを書くSkillは作成しても良いと思った。
    - 完了したタスクを整理して、Issueを編集、クローズするSkillも作成しても良いと思った。
- AIと用語の定義をすり合わせるのに、結構時間がかかった。
    - ただし、この用語の定義はとても大切であると感じたので、時間をかけるべきだと思った。
    - チームでも、設計と実装の前に、用語の定義は議論してすり合わせる必要があると感じた（じゃないと議論をするときに、認識がずれる）。
    - 一般用語はまだいいかもしれないが、このアプリで使う専門用語は、特に用語集を作成して定義をすり合わせておいた方が良い。
- AIの弱点として、同じ議論の最中に、同じ概念に対して違う言葉で議論してくる気がした。
    - その結果、論点がずれていくことがあった。
    - 新しい用語に関しては、逐一用語集に追加して、AIに参照させて議論する必要があると感じた。
--------------
### 2026/6/3
#### やったこと
- プロトタイプのロードマップ作成
#### 気づき
- プロトタイプのロードマップ作成
    - ロードマップを作成したが、**フェーズごとにどのようなリスクを潰していくのかが分かるロードマップにすることが重要である**と思った。
    - 特に、**大きなリスクを最初に潰していくことが重要**だと感じた。
--------------
### 2026/6/4
#### やったこと
- phase0の進め方の詳細を決定
- pythonの環境構築
#### 気づき
- pythonの環境構築
    - pythonの仮想環境にはuvを使用することとした。
    - 今まではvenvで管理していたが、venvは有効化が必要だった。
    - uvは有効化が不要なので、良いと思った。
    - これからはuv中心で使っていきたい。
--------------