# 設計決定ログ（Decision Log）

> このドキュメントは FoleyForge の「決めたこと」と「その理由」を残す台帳です。
> 進め方の約束事（ラベル・Issue 運用など）は [dev-workflow.md](./dev-workflow.md) を、開発の全体像・方針は [foley-forge-dev.md](./foley-forge-dev.md) を、これから決める／作る未決事項は GitHub Issues を参照してください。

このドキュメントは**追記のみ・上書き禁止**です。決定を見直した場合は、古いエントリを `Superseded` にして新エントリを追記します（運用は dev-workflow.md の「後戻り」を参照）。

---

## 決定一覧

| ID | 決定 | 理由（要約） | 状態 | 関連 Issue | 日付 |
|----|------|------------|------|-----------|------|
| FF-D001 | Issue / ラベル / 決定ログの3レイヤー運用を採用する | GitHub Issues は未決を追うのは得意だが、決定理由を俯瞰的に残すのは不得意。「追うもの（Issues）」と「残すもの（decisions.md）」を分離し、dev.md は全体像に専念させることで、二重管理とドリフトを防ぐ。運用の詳細は dev-workflow.md | Accepted | — | 2026-05-31 |
| FF-D002 | LLM プロバイダー（Step 2/3）を抽象化レイヤーで切替可能にする | クラウド品質（Claude 等）とローカル完結（オフライン・コスト0）を両立。OSS 公開時の採用障壁も下げる。Claude をリファレンス（品質の基準線）とする | Accepted | — | 2026-05-31 |
| FF-D003 | T2A モデル（Step 5）を複数切替可能にし、設定で仕様を管理する | モデルごとに音色・条件付け・最適パラメータが異なるため、生成アルゴリズム本体から分離。「拡散モデル」ではなく flow matching・自己回帰も含む「T2A モデル」として抽象化する | Accepted | — | 2026-05-31 |
| FF-D004 | モデル本体は配布・同梱しない（ユーザが各自ダウンロードして配置） | モデルごとのライセンスの複雑さを回避。配布しないことでライセンス確認の負荷も消える | Accepted | — | 2026-05-31 |
| FF-D005 | Step 6（評価）は LLM を使わず決定論的に行う | CLAP/PAM は対比学習ベースの埋め込みモデルで、同じ入力なら必ず同じスコアを返す。生成系（Step 2/5・確率的）とは性質が異なる | Accepted | — | 2026-05-31 |
| FF-D006 | GPU 最適化に ComfyUI を採用しない | ComfyUI の最適化の大半は diffusers/PyTorch が提供する範囲で代替可能。SE 生成特化アプリにとって、ユーザに ComfyUI 学習を強いるデメリットが上回る。詳細は [local-inference-optimization-strategy.md](../research/gpu-optimization/local-inference-optimization-strategy.md) | Accepted | — | 2026-05-31 |
| FF-D007 | 開発・配布で Docker を前提にしない | 想定ユーザーは非エンジニア。Docker 環境構築の負荷と、パラメータ調整のデバッグのしづらさを回避。OSS として進む場合に再検討 | Accepted | — | 2026-05-31 |
| FF-D008 | 個人パーソナライズは段階導入（Tier0→1→2）とし、初期段階では自動調整（自動パーソナライズ）を採用しない | training-free の利点（安定性・再現性・デバッグ容易）を守るため。proxy（CLAP/PQ）はノイズがあり好みは非定常で、自動連続適応は制御的に発振・ドリフトしうる（「昨日良くて今日ダメ」）。生成は元々確率的で、自動ドリフトが乗ると原因帰属が不能に。採用率の自動追従は多様性を縮退させる（degenerate feedback loop）。silent な自動変化は最小驚き原則に反する。現段階はデータ0・ユーザ1人で YAGNI（手動が頭打ちの証拠が出てから／philosophy §4・§9）。**初期は Tier0（固定既定値）**で開始し、**Tier1（例ベースのプリセット＝ユーザ主導・可逆）→ Tier2（加算的に自動で足す）**の順で段階導入。全段で**加算的探索**（グローバルゲイン固定＋ローカルゲインを足す）を採り、自動連続ドリフトはしない。可変範囲は C/D 限定（A/B 不変）。詳細は観測・評価設計 §10.1〜§10.3 | Accepted | — | 2026-06-02 |
| FF-D009 | 既定候補の表示数 **F** と**戦略数**を概念分離し、MVP は **F=3（各戦略1個ずつ＝配分1:1:1）** を既定とする。バリエーション数のユーザ可変化・配分変更（例 2:2:1 で F>3）は将来 | 「戦略数=3」と「表示数 F」を同一視していたため dev Step1（ユーザ指定のバリエーション数）・Step7（配分2:2:1）・観測評価 §10.3（F=固定3）が不整合だった。戦略は候補の“味”の種類、F は実際に見せる既定候補の数、と分離。MVP は最小構成（各戦略1個）で確定し、可変化は YAGNI で後回し。あわせて**表示数（F＋K）と生成数（Best-of-N）を区別**し、用語を**既定候補（土台）／個人候補（上積み）**に統一。詳細は観測・評価設計 §10.3 | Accepted | — | 2026-06-03 |
| FF-D010 | Step 3（構造化データ→プロンプト生成）は**決定論アルゴリズム（プロンプトビルダー＝テンプレート＋統制語彙）**で行い、オンラインで LLM を挟まない | LLM の拡張価値（曖昧入力→豊かなキャプション）は Step2 に既にあり、Step3 は**忠実な合成**＝決定論の領分。確率化は追跡性・再現性・credit assignment を損ね、**LLM はハルシネーション（音源の捏造・取りこぼし）面を増やす**のに、多様性は増えない（3戦略＋Best-of-N＋Step5 が供給済み）。Stable Audio の推奨 SE プロンプト構造（源＋動作＋特性＋品質＋ネガティブ）もスキーマと一致しテンプレで命中可。原則「確率性は解釈/多様性に、忠実/追跡/再現は決定論に」（観測評価 §12 原則9）。**留保（過信しない）**：テンプレ品質が流暢な LLM 出力に劣りうるため、ビルダーは抽象化の裏に置き差し替え可能にし、LLM は開発時（オフライン）の語彙・テンプレ設計に使う。オフライン A/B（採用率）で頭打ちなら再検討。前提：Step2 スキーマが豊かな修飾語を吐くこと。詳細は dev §3.1/§3.2/§4・観測評価 §12/§14・approach-1 | Accepted | — | 2026-06-03 |

> 状態の凡例: `Accepted`（有効）/ `Superseded`（新しい決定に置き換え。置き換え先の ID を理由欄に記す）/ `Deprecated`（廃止）

---

## 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-05-31 | 初版作成。既存の確定済み設計判断（FF-D001〜D007）を初期エントリとして記録。運用ルールは conventions.md に分離 |
| 2026-05-31 | 運用ルールのドキュメントを `conventions.md` から `dev-workflow.md` に改名し、タイトルを「開発の進め方ルール（Issue・ラベル・決定ログ）」に変更。Issue 運用に限らず情報の3レイヤー全体を束ねるルールであることを名前で示すため |
| 2026-05-31 | dev-workflow.md §4 を Issue ライフサイクル（起票・クローズ・削除・後戻り）として整理。クローズ理由を completed / not planned で使い分け、「見送り」は閉じず open 維持、`type:` 別のクローズ作法と「原則削除しない」を明記。`status:undecided` ラベルを廃止し未決一覧を `is:open` に統一（open=未決のため重複） |
| 2026-06-01 | §3 の色ルールを原理ベースに整理（比較できない `type`/`phase` はグループ内で色を揃え、比較できる `priority` は段階色）。GitHub 側の `type:` ラベル色も緑に統一。あわせて §3 のグループ列挙に `priority` を追加（列挙漏れの修正） |
| 2026-06-02 | FF-D008 を追記。個人パーソナライズを段階導入（Tier0→1→2）とし、初期は自動調整を採用しない方針を決定。観測・評価設計 §10.1〜§10.3 と連動 |
| 2026-06-03 | FF-D009 を追記。既定候補の表示数 F と戦略数を概念分離し、MVP は F=3（各戦略1個ずつ＝1:1:1）を既定に。用語を既定候補（土台）／個人候補（上積み）に統一し、表示数（F＋K）と生成数（Best-of-N）を区別。観測・評価設計 §10.3／dev Step1・Step7 と連動 |
| 2026-06-03 | FF-D010 を追記。Step3 を決定論アルゴリズム（プロンプトビルダー）に確定し、オンライン LLM を不採用。原則「確率性は解釈/多様性に置き、忠実/追跡/再現は決定論に」を観測評価 §12 原則9 に明文化。dev §3.1/§3.2/§4・観測評価 §9/§14 を更新 |
